[database]
dbname = "danbooru"
user = "postgres"
password = "postgres"

[file_names]
posts = "posts.json"
tags = "tags.json"
artists = "artists.json"
artist_urls = "artist_urls.json"
tag_aliases = "tag_aliases.json"
tag_implications = "tag_implications.json"

[insertion]
batch_count = 1000

